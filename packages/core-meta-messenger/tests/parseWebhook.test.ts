import { describe, expect, it } from 'vitest';

import { parseMessengerWebhook } from '../src/index.js';

describe('parseMessengerWebhook (scaffold)', () => {
  it('throws TODO to signal missing real fixtures', () => {
    expect(() => parseMessengerWebhook({})).toThrow(/TODO/);
  });
});
